{
  "name": "gwc-react-tutorial",
  "version": "0.2.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.11.9",
    "@testing-library/react": "^11.2.5",
    "@testing-library/user-event": "^12.8.1",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-scripts": "4.0.3",
    "web-vitals": "^1.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "intro": "node -e 'require(\"./utils/instructions\").intro()'",
    "lesson1": "npm run lesson1:instr && npm run lesson1:test",
    "lesson1:instr": "node -e 'require(\"./utils/instructions\").lesson1()'",
    "lesson1:test": "npm test -- -t (.*l01.*)",
    "lesson2": "npm run lesson2:instr && npm run lesson2:test",
    "lesson2:instr": "node -e 'require(\"./utils/instructions\").lesson2()'",
    "lesson2:test": "npm test -- -t (.*l02.*)"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
